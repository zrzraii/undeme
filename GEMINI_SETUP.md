# Настройка Gemini API

## Как получить API ключ

1. Перейдите на сайт: **https://makersuite.google.com/app/apikey**
2. Войдите с вашим Google аккаунтом
3. Нажмите "Create API Key"
4. Скопируйте ключ

## Способы использования ключа

### Способ 1: Через командную строку (Самый простой)

Запустите приложение с ключом:

```bash
flutter run --dart-define=GEMINI_API_KEY=ваш_ключ_здесь
```

**Примеры:**

```bash
# iOS симулятор
flutter run -d iPhone --dart-define=GEMINI_API_KEY=AIzaSyD...

# macOS
flutter run -d macos --dart-define=GEMINI_API_KEY=AIzaSyD...

# Android
flutter run -d android --dart-define=GEMINI_API_KEY=AIzaSyD...
```

### Способ 2: Через файл конфигурации (Удобнее для разработки)

1. Откройте файл: `lib/config/api_keys.dart`

2. Замените строку:
   ```dart
   static const String geminiApiKey = 'ВСТАВЬТЕ_ВАШ_КЛЮЧ_СЮДА';
   ```
   
   На:
   ```dart
   static const String geminiApiKey = 'AIzaSyD...ваш_реальный_ключ';
   ```

3. Обновите `lib/screens/chat_screen.dart` для использования этого файла:
   ```dart
   import '../config/api_keys.dart';
   
   // В методе initState():
   final apiKey = envApiKey.isNotEmpty ? envApiKey : ApiKeys.geminiApiKey;
   ```

4. Запустите приложение обычным способом:
   ```bash
   flutter run
   ```

### Способ 3: Через переменные окружения (Для продакшена)

1. Создайте файл `.env` в корне проекта:
   ```
   GEMINI_API_KEY=ваш_ключ_здесь
   ```

2. Добавьте `.env` в `.gitignore`

3. Используйте пакет `flutter_dotenv` для загрузки:
   ```bash
   flutter pub add flutter_dotenv
   ```

## Проверка работы

После настройки ключа:

1. Запустите приложение
2. Перейдите на вкладку "AI Чат"
3. Задайте вопрос, например: "Какие у меня права при остановке полицией?"
4. AI должен ответить на ваш вопрос

Если ключ не настроен, вы увидите сообщение:
> "Пожалуйста, настройте Gemini API ключ для включения AI-ответов. Для экстренной помощи звоните 112."

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте API ключи в Git!
- Файл `lib/config/api_keys.dart` уже добавлен в `.gitignore`
- Для продакшена используйте переменные окружения
- Не делитесь своим ключом публично

## Ограничения бесплатного плана

Gemini API бесплатный план:
- 60 запросов в минуту
- 1500 запросов в день
- Достаточно для разработки и тестирования

## Troubleshooting

### Ошибка: "API key not valid"
- Проверьте, что ключ скопирован полностью
- Убедитесь, что ключ активен в Google AI Studio

### Ошибка: "Quota exceeded"
- Вы превысили лимиты бесплатного плана
- Подождите до следующего дня
- Или перейдите на платный план

### AI не отвечает
- Проверьте подключение к интернету
- Убедитесь, что ключ правильно настроен
- Проверьте логи Flutter для детальной информации

## Дополнительная информация

- Документация Gemini: https://ai.google.dev/docs
- Google AI Studio: https://makersuite.google.com
- Примеры использования: https://ai.google.dev/tutorials

---

**Готово!** После настройки ключа AI помощник будет работать полностью. 🤖
